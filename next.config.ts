/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ["en", "es", "pt", "pt-BR", "zh"],
  //   defaultLocale: "es",
  // },
  // i18n: false,
  reactStrictMode: true,
};

module.exports = nextConfig;
